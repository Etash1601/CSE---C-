#include <stdio.h>
#include <conio.h>
int main()
{
	int a,b,c;
	printf("Enter values for a and b");
	scanf("%D %D",&a,&b);
	if(a==100)
	{
		if(b==200)
	{
		printf("The value of a is 100 and b is 200 \n",a,b);
	}
}
{
		printf("User value of a is %d and of b is %d",a,b);
	}
    return 0;
}
